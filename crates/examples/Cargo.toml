[package]
name = "graphium-examples"
version = "0.1.0"
edition = "2024"

[dependencies]
axum = "0.7"
tokio = { version = "1", features = ["macros", "rt-multi-thread", "net"] }

# graphium crates
graphium = { version = "0.1.0", path = "../core" }
graphium-macro = { version = "0.1.0", path = "../macro" }
graphium-ui = { version = "0.1.0", path = "../ui" }

