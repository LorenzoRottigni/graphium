[package]
name = "graphio-examples"
version = "0.1.0"
edition = "2024"

[dependencies]
axum = "0.7"
tokio = { version = "1", features = ["macros", "rt-multi-thread", "net"] }

# graphio crates
graphio = { version = "0.1.0", path = "../core" }
graphio-macro = { version = "0.1.0", path = "../macro" }
