# Graphium Core (Runtime)

The project at current status only provides real performance.

Everything happens at compile time - isn't provided any shared runtime contract.

The future of the core crate is to optionally sacrifice performance to move the abstraction at runtime, using a `Controller` Orchestrating nodes executions, artifacts wiring, control flow modifiers, ...
