[package]
name = "graphium-ui"
version = "0.1.0"
edition = "2024"

[lib]
doctest = false

[dependencies]
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
graphium-ui-next = { version = "0.1.0", path = "../ui-next" }
