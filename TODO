# Graphium

- allow event bus publish like:
    node! {
        #[publish = EventName]
        async fn get_number() -> u32 {
            7
        }
    }
- allow nodes explicit naming:
    node! {
        #[name = GetNumber]
        async fn get_number() -> u32 {
            7
        }
    }
